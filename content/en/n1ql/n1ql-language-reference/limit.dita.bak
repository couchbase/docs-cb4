<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept id="concept_dsd_hgk_np">
 <title>LIMIT Clause</title>

 <conbody>
<codeblock>LIMIT expression</codeblock>
  <p>The LIMIT clause specifies the maximum number of objects that can be returned in a result set by SELECT. 
   This clause must have a non-negative integer as its upper bound. </p>
  
   <p><b>Name and age list: limit by 2</b></p>
   <codeblock>SELECT fname, age
     FROM tutorial
          ORDER BY age
              LIMIT 2 
</codeblock>  
   <p>Returns:</p>
   <codeblock>
 {
   "resultset": [
     {
       "age": 18,
       "fname": "Fred"
     },
     {
       "age": 20,
       "fname": "Harry"
     }
   ]
 }    
  </codeblock>  
  
 </conbody>
</concept>
