<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept id="concept_npj_srf_2s">
	<title>USE INDEX clause</title>
	<shortdesc>The <codeph>USE INDEX</codeph> clause specifies which index to use as part of the query
		execution. The query engine attempts to use the specified index if the index is applicable
		for the query.</shortdesc>
	<conbody>
		<p><b>Syntax</b></p>
		<codeblock>USE INDEX ( <varname>index-ref1</varname> [ , <varname>index-ref2</varname> ]* ) [ USING ( VIEW | GSI ) ]</codeblock>
		<dl>
			<dlentry>
				<dt>Arguments</dt>
				<dd>
					<dl>
						<dlentry>
							<dt>index-ref</dt>
							<dd>[Required] String or expression representing the index to be used
								for the query.</dd>
						</dlentry>
					</dl>
					<dl>
						<dlentry>
							<dt><codeph>USING</codeph></dt>
							<dd>[Optional] Specifies how to use the index</dd>
							<dd>
								<dl>
									<dlentry>
										<dt><codeph>VIEW</codeph></dt>
										<dd>The View setting.</dd>
									</dlentry>
								</dl>
								<dl>
									<dlentry>
										<dt><codeph>GSI</codeph></dt>
										<dd>Uses the Global Secondary Index</dd>
									</dlentry>
								</dl>
							</dd>
						</dlentry>
					</dl>
				</dd>
			</dlentry>
		</dl>  
		<p>Example 1: </p> 
		<p>a</p>
		<codeblock>a</codeblock>
		
		<p>Here's an example that shows how to use index hints. The example creates a GSI index called
				<codeph>beer_abv</codeph>, and then selects the <codeph>name</codeph> and
				<codeph>abv</codeph> fields, specifying that the query engine use the
				<codeph>beer_abv</codeph> index.</p>
		<codeblock outputclass="language-sql">CREATE INDEX beer_abv ON `beer-sample`(abv) USING GSI;

SELECT name, abv FROM `beer-sample` USE INDEX (beer_abv USING GSI) WHERE abv > 10;			</codeblock>
		<p>The following example shows how to perform a similar query with a view index: </p>
		<codeblock outputclass="language-sql">CREATE INDEX beer_abv ON `beer-sample`(abv) USING VIEW;

SELECT name, abv FROM `beer-sample` USE INDEX (beer_abv USING VIEW) WHERE abv > 10;			</codeblock>
	</conbody>
</concept>
